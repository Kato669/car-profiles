class Car {
  String? carEngine;
  String? color;
  String? carNumber;

  Car({String? engine, String? colour, String? numbr}) {
    carEngine = engine;
    color = colour;
    carNumber = numbr;
  }
}
